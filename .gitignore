/node_modules
/public/hot
/public/storage
/storage/*.key
/storage
/storage/debugbar/*
/vendor
.env
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode

# Project-specific ignores
testsargam2_dump1811_20012026.sql
docker-compose.yml
/java-backup
/java-project
/nginx.conf
/praposed-mess-management-sidebar-option.jpeg
/LBSNAA
/SECURITY_MODULE_IMPLEMENTATION.md
/ISSUE_REPORT_NOTIFICATIONS.md
.htaccess
# A nested tool install is never committed: only the manifest and lock that
# pin it are. One stray vendor file used to be tracked here, which could not
# run anything and drifted from tools/phpstan/composer.lock on every install.
tools/phpstan/vendor/
# Playwright writes these on every run; they are generated artifacts, not source.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
bootstrap/
tools/phpstan/
